<ui:FluentWindow x:Class="MediaHub.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:MediaHub.Views.Modules"
        Title="MyApp" Height="640" Width="980"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <ui:FluentWindow.Resources>
        <Style x:Key="TopTabButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background"
                                        Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TopTabButtonActive" TargetType="Button" BasedOn="{StaticResource TopTabButton}">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="{DynamicResource SystemAccentColorBrush}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background"
                                        Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== 顶部栏 ===== -->
        <Border Grid.Row="0"
                Background="{DynamicResource NavigationViewTopPaneBackground}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                <!-- 左侧 Logo + 名称 -->
                <StackPanel DockPanel.Dock="Left" Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="12,0,8,0">
                    <ui:SymbolIcon Symbol="Heart12"
                                   FontSize="18"
                                   Filled="True"
                                   Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                    <TextBlock Text="MyApp"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
                    
                <!-- 右侧窗口控制按钮 -->
                    <ui:TitleBar Grid.Column="2"
                       ShowMinimize="True"
                       ShowMaximize="True"
                       ShowClose="True"/>

                            <!-- 中间 Tab 按钮 -->
                    <StackPanel Orientation="Horizontal" Grid.Column="1"
                      VerticalAlignment="Stretch"
                      Margin="8,0,0,0">
                                <Button x:Name="BtnMusic"
                      Content="🎵  音乐"
                      Style="{StaticResource TopTabButtonActive}"
                      Click="BtnMusic_Click"/>
                                <Button x:Name="BtnMovie"
                      Content="🎬  电影"
                      Style="{StaticResource TopTabButton}"
                      Click="BtnMovie_Click"/>
                            <Button x:Name="BtnPodcast"
                      Content="🎙  播客"
                      Style="{StaticResource TopTabButton}"
                      Click="BtnPodcast_Click"/>
                    </StackPanel>
                </Grid>
        </Border>

        <!-- ===== 内容区 ===== -->
        <Grid Grid.Row="1" x:Name="ContentGrid">
            <!-- ShellView 由 code-behind 注入 -->
        </Grid>

    </Grid>
</ui:FluentWindow>
