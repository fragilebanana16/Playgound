<UserControl
    x:Class="MediaHub.Views.Modules.MusicShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MediaHub.Music.Controls"
    xmlns:pages="clr-namespace:MediaHub.Music.Views"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <!--  控制栏  -->
        </Grid.RowDefinitions>

        <ui:NavigationView
            x:Name="MusicNavView"
            IsBackButtonVisible="Collapsed"
            IsPaneOpen="True"
            OpenPaneLength="200"
            PaneDisplayMode="Left"
            Transition="None">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="主页"
                    Icon="{ui:SymbolIcon Home24}"
                    TargetPageType="{x:Type pages:MusicHomePage}" />
                <ui:NavigationViewItem
                    Content="最近播放"
                    Icon="{ui:SymbolIcon History24}"
                    TargetPageType="{x:Type pages:MusicRecentPage}" />
                <ui:NavigationViewItem
                    Content="我的收藏"
                    Icon="{ui:SymbolIcon Heart24}"
                    TargetPageType="{x:Type pages:MusicFavoritePage}" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="设置" Icon="{ui:SymbolIcon Settings24}" />
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>

        <controls:ControlBarControl Grid.Row="1" />
    </Grid>

</UserControl>
